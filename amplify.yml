version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build steps required"  # No build necessary, frontend is a single file
  artifacts:
    baseDirectory: .
    files:
      - frontend
  cache:
    paths:
      - node_modules/**/*
